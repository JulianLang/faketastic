/** The indentifiers for attached properties set by faketastic's attached functions. */
export const AttachedProperties = {
  range: {
    /** Set by attached range's strategy() attached-function. */
    strategy: 'faketastic:range.strategy',
  },
};
